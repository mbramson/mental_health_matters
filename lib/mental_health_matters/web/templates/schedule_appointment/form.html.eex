<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :coach_id, class: "control-label" %>
    <%= select f, :coach_id, data_as_select(@coaches), prompt: "(no coaches available)", class: "form-control" %>
    <%= error_tag f, :coach_id %>
  </div>

  <div class="form-group">
    <%= label f, :meeting_time, class: "control-label" %>
    <%= text_input f, :meeting_time, class: "form-control" %>
    <%= error_tag f, :meeting_time %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
